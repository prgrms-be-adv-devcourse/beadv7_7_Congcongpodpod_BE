spring:
  application:
    name: member-service
  cloud:
    config:
      enabled: false

member:
  message: Hello from test config

management:
  endpoints:
    web:
      exposure:
        include: health,info,refresh
