spring:
  application:
    name: core-service
  cloud:
    config:
      enabled: false

core:
  message: Hello from test config

management:
  endpoints:
    web:
      exposure:
        include: health,info,refresh
