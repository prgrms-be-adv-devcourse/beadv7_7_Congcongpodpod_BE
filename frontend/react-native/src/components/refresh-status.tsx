// Pull-to-refresh keeps its gesture only. API activity is presented by the
// shared centered loading popup so headers never move or duplicate feedback.
export function RefreshStatus({ visible: _visible }: { visible: boolean }) {
  return null;
}
